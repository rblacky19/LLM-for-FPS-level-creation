FPS_Level_Evaluation_Oral_Interview_Guide_v3.docx:
The interview sheet I used to interview evaluators over Discord call.

JSON_to_vmap.py:
The final step of both pipelines, converting JSON format to actual .vmap files.

Level creation instructions.zip:
The instructions the map builders got to create the human created maps.

Pipeline 1.py:
The full LLM pipeline

Pipeline2_Solver.py: 
The pipeline that uses the solver algorithm

Prompt.txt:
The design brief used for both pipelines and the human designers

nl_to_solver_input_parameters.py:
The step BEFORE the solver algorithm to go from the natural language input (prompt) to solver parameters.

template40x40.vmap:
The template that the human designers used to build their levels.

